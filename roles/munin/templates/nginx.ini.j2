server {
    listen {{munin_port}};    

    location / {
            root /var/cache/munin/www;
            auth_basic      "Restricted";
            auth_basic_user_file    /etc/nginx/passwdfile;
    }
}